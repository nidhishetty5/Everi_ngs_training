import { NamDtvDirective } from './nam-dtv.directive';

describe('NamDtvDirective', () => {
  it('should create an instance', () => {
    const directive = new NamDtvDirective();
    expect(directive).toBeTruthy();
  });
});
